'use strict';

const tanstackRouterSandbox = require('..');
const assert = require('assert').strict;

assert.strictEqual(tanstackRouterSandbox(), 'Hello from tanstackRouterSandbox');
console.info('tanstackRouterSandbox tests passed');
